defaults: base.yaml
run_name: "B1_random_init_baseline"
data:
  target: eightclass_yaml_path
  eightclass_yaml_path: "data/eightclass/datasets/RFT.yaml"
model:
  use_cifr: false
  pretrained_backbone: null
  img_size: 480
train:
  finetune_strategy: "full_finetune"
  device: 0
  cache: true
wandb:
  tags: ["baseline", "phase1", "random-init"]
